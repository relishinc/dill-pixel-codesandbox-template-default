import { Application } from 'dill-pixel';

import { type ActionTypes, type Contexts, type Data } from '@/dill-pixel.config';

// TODO: Add custom application logic here (optional)
export class MyApplication extends Application<Data, Contexts, ActionTypes> {}
