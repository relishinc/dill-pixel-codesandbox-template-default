{
  "name": "dill-pixel-codesandbox-template-default",
  "description": "Dill Pixel default template for CodeSandbox",
  "version": "0.0.0",
  "private": false,
  "main": "index.html",
  "scripts": {
    "clean": "rimraf .assetpack .cache dist",
    "start": "pnpm clean && dill-pixel start",
    "dev": "pnpm clean && dill-pixel dev",
    "preview": "pnpm clean && dill-pixel preview",
    "build": "pnpm clean && dill-pixel build"
  },
  "dependencies": {
    "dill-pixel": "^6.2.2"
  },
  "peerDependencies": {
    "@pixi/sound": "^6.0.1",
    "gsap": "^3.13.0",
    "pixi.js": "^8.10.2"
  }
}
